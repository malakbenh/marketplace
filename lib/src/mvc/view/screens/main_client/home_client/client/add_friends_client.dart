import 'package:flutter/material.dart';

class AddFriendClient extends StatelessWidget {
  const AddFriendClient({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Friends'),
      ),
    );
  }
}
