import 'package:flutter/material.dart';

class NotificationsClient extends StatelessWidget {
  const NotificationsClient({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Notifications'),
      ),
    );
  }
}
