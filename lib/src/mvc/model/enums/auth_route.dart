/// presents the diffrent auth routes.
enum AuthRoute {
  /// use signin route
  signin,

  /// use signup route
  register,

  /// use forgetpassword route
  forgotpassword,

  /// use activation route
  activation,
  login_or_signup_screen,
  selectType,
}
